# trello2markdown

Converts a JSON export from Trello to a Markdown file. WIP.

## Usage

```python
python trello2markdown.py <FILENAME>
```

Make sure `FILENAME` points to a dump from Trello's export feature.
